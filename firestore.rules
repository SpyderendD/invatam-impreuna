rules_version = '2';
service firebase.storage {
match /b/{bucket}/o {
match /avatars/{uid} {
allow write: if request.auth != null && request.auth.uid == uid;
allow read: if true; // sau restrânge după nevoie
}
match /{allPaths=**} {
allow read: if false;
allow write: if false;
}
}
}